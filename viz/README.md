# BespokePlanet visualizer

`output/` に保存された順圧渦度方程式の計算結果を、3D 地球・2D 正距円筒図法・保存量診断で表示します。

## 起動

```sh
cd viz
npm install
python3 server.py
```

ブラウザで <http://127.0.0.1:8000> を開いてください。別の出力ディレクトリを読む場合は `--output` を指定できます。

```sh
python3 server.py --output /path/to/output --port 8080
```

Python 側には外部パッケージは不要です。Three.js は `npm install` で `viz/node_modules/` に配置され、Python サーバーからローカル配信されます。

## 表示内容

- 全格子点の流速絶対値を、低速の青から高速の赤までの色で表示
- 同一フレームを回転可能な 3D 球面と 2D 正距円筒図法に表示
- フレーム再生、時刻スクラブ、再生速度変更
- Gaussian 求積による平均運動エネルギー、平均絶対エンストロフィー、平均相対渦度、平均相対軸角運動量の時系列

アニメーション中に色が脈動しないよう、カラースケール上端はラン全体の最大流速で固定します。最大流速はメタデータに保存された最大 CFL から復元します。古いメタデータなどで復元できない場合だけ、最初に表示したフレームの 98 パーセンタイルを固定値として使います。`MAX SPEED` は各フレームの実際の最大値です。
